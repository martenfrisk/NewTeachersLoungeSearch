[install]
# Only install package versions that have been published for at least 14
# days, to reduce exposure to supply-chain attacks where a compromised
# version gets published and pulled by installs before it's caught.
minimumReleaseAge = 1209600
